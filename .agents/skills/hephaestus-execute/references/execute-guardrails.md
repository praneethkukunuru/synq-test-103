# Execute Guardrails

- Every change references a spec section.
- Every commit scope maps to one plan slice when possible.
- Keep execution notes in `.hephaestus/logs/` to prevent context drift.
- Route back to `hephaestus-spec-plan` if architecture drift appears.
